{{ form_start(form) }}
    {{ form_widget(form) }}
    {% if app.user %}
        <p>afficher formulaire Comment</p>
    {% endif %}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
